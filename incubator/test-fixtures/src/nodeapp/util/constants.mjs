// @ts-check
/**
 * Raw ES module file (not TypeScript). Tests bundler handling of raw `.mjs`
 * source. Type-checked via `checkJs: true` + JSDoc.
 */

/** @type {number} */
export const EPSILON = 1e-9;

/** @type {number} */
export const DEFAULT_WINDOW = 4;

/** @type {number} */
export const DEFAULT_TOP_K = 5;

/** @type {"population" | "sample"} */
export const DEFAULT_STDDEV_MODE = "population";

/** @type {number} */
export const ROUND_DECIMALS = 6;
