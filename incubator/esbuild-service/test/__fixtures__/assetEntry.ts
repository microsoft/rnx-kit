// This fixture imports an asset (PNG), which triggers the assets plugin.
// The import will be resolved to a registerAsset() call in the bundle.
const icon = require("./icon.png");
export default icon;
