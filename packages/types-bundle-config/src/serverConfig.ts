import type { BundleParameters } from "./bundleConfig.ts";

/**
 * Parameters controlling how the bundle server is run.
 *
 * This is an alias for {@link BundleParameters}, retained for backwards
 * compatibility.
 */
export type ServerConfig = BundleParameters;
