---
"@rnx-kit/align-deps": minor
---

Add a read-only `--why <package>` command to explain which packages and
capabilities require a dependency.

Inspect separately installed dependency instances independently, deduplicate
explicit package paths, and preserve failures for invalid paths while continuing
to inspect valid packages. Support required catalog paths in the Commander
adapter.
